# 🚀 JavaScript Fundamentals — 1


---

## 🌟 1. Introduction to JavaScript

JavaScript is the **brain inside the browser**.

Think about it:

* Every button click 🖱️
* Every animation 🎬
* Every interactive website 🌐
* Every web or mobile app 📱

All of it works because of **JavaScript**.

### 🧠 How to Think About JavaScript

Imagine a **workshop full of machines**:

* Machines = Variables, Arrays, Functions
* Instructions = JavaScript code
* Power source = Browser Console / Node.js / VS Code

Just like machines need instructions to work, your browser needs JavaScript to behave dynamically.

---

# 💻 2. Running JavaScript

You can run JavaScript in:

* Browser Console
* Node.js
* VS Code (with Node installed)

### ▶ Example: First Program

```js
console.log("Hello World");
```

Run using:

```bash
node hello.js
```

### 🔎 What is `console.log()`?

`console.log()` is a built-in JavaScript method used to print output to:

* Terminal (Node.js)
* Browser Console

---

# 📦 3. Variables in JavaScript

Variables are like **labeled boxes** that store data.

JavaScript provides **3 ways** to declare variables:

| Keyword | Can Reassign? | Scope          | Modern Usage  |
| ------- | ------------- | -------------- | ------------- |
| `var`   | ✅ Yes         | Function Scope | ❌ Avoid       |
| `let`   | ✅ Yes         | Block Scope    | ✅ Recommended |
| `const` | ❌ No          | Block Scope    | ✅ Recommended |

---

## 🔹 `let` Example

```js
let message = "Hello";
message = "Hi";
```

✔ Allowed because `let` can be reassigned.

---

## 🔹 `const` Example

```js
const PI = 3.14;
PI = 3.1415; // ❌ Error
```

🚫 Constants cannot be reassigned.

---

## 🔥 Important: `var` vs `let` (Scope Difference)

### Example with `var`

```js
var x = 10;

if (true) {
    var x = 20;
}

console.log(x); // 20
```

⚠ `var` changes the global variable.

---

### Example with `let`

```js
let x = 10;

if (true) {
    let x = 20;
}

console.log(x); // 10
```

✔ `let` is block-scoped.

---

# 🔁 4. Functions in JavaScript

A **function** is a reusable block of code.

### Why use functions?

Instead of writing the same code multiple times:

❌ Rewriting code
✔ Write once → Reuse many times

---

## Example Function

```js
function printNumber(a) {
    console.log(a);
}
```

Calling it:

```js
printNumber(10);
printNumber(20);
printNumber(30);
```

---

# 🔀 5. Conditional Statements

Conditional statements allow decision-making.

---

## 🟢 `if` Statement

```js
if (a === 10) {
    console.log("Value is 10");
}
```

---

## 🔵 `if...else`

```js
if (a === 10) {
    console.log("Value is 10");
} else {
    console.log("Value is not 10");
}
```

---

## 🟣 `if...else if...else`

```js
if (a === 10) {
    console.log("Value is 10");
} else if (a === 20) {
    console.log("Value is 20");
} else {
    console.log("Value is neither 10 nor 20");
}
```

---

# 🎓 6. Project: Student Grade Calculator

Now comes the practical application of everything learned!

---

# 📚 CONSTANTS

```js
const subjects = ["Math", "Science", "English", "History", "Programming"];
const HONOR_ROLL_THRESHOLD = 90;
const PASSING_GRADE = 60;
```

### Why `const`?

Because:

* Subjects don’t change
* Honor threshold is fixed
* Passing grade is fixed

---

# 🧮 Calculate Average Function

```js
function calculateAverage(grades) {
    let total = 0;

    for (let i = 0; i < grades.length; i++) {
        total += grades[i];
    }

    return total / grades.length;
}
```

### 📌 What happens here?

1. Start with total = 0
2. Loop through grades
3. Add each grade
4. Divide by number of subjects

---

# 🎯 Determine Letter Grade

```js
function determineLetterGrade(average) {
    if (average >= 90) return "A";
    else if (average >= 80) return "B";
    else if (average >= 70) return "C";
    else if (average >= 60) return "D";
    else return "F";
}
```

### Grading Scale Logic

| Marks  | Grade |
| ------ | ----- |
| 90–100 | A     |
| 80–89  | B     |
| 70–79  | C     |
| 60–69  | D     |
| < 60   | F     |

---

# 🏅 Honor Roll Check

```js
function isHonorRoll(average) {
    return average >= HONOR_ROLL_THRESHOLD;
}
```

Returns:

* `true` if ≥ 90
* `false` otherwise

---

# ✅ Passing Check

```js
function isPassing(average) {
    return average >= PASSING_GRADE;
}
```

Returns:

* `true` if ≥ 60
* `false` otherwise

---

# 🖨 Generate Report Card

This function:

1. Prints student details
2. Prints subject-wise marks
3. Calculates average
4. Determines grade
5. Checks honor roll
6. Checks pass/fail
7. Prints congratulations if eligible

---

# 👨‍🎓 Student Data Structure

```js
const students = [
    {
        name: "Johnson",
        id: "S101",
        grades: [95, 88, 92, 85, 90]
    }
];
```

Each student object contains:

* `name`
* `id`
* `grades` array

---

# 📊 Class Statistics Logic

We calculate:

* Total Class Average
* Honor Roll Count
* Passing Count

Using a loop:

```js
for (let i = 0; i < students.length; i++) {
    const result = generateReportCard(students[i]);
}
```

---
# ✅ Full Working Code

```js
// ===============================
// 📚 CONSTANTS
// ===============================

const subjects = ["Math", "Science", "English", "History", "Programming"];
const HONOR_ROLL_THRESHOLD = 90;
const PASSING_GRADE = 60;


// ===============================
// 🧮 FUNCTION: Calculate Average
// ===============================

function calculateAverage(grades) {
    let total = 0;

    for (let i = 0; i < grades.length; i++) {
        total += grades[i];
    }

    return total / grades.length;
}


// ===============================
// 🎯 FUNCTION: Determine Letter Grade
// ===============================

function determineLetterGrade(average) {
    if (average >= 90) return "A";
    else if (average >= 80) return "B";
    else if (average >= 70) return "C";
    else if (average >= 60) return "D";
    else return "F";
}


// ===============================
// 🏅 FUNCTION: Check Honor Roll
// ===============================

function isHonorRoll(average) {
    return average >= HONOR_ROLL_THRESHOLD;
}


// ===============================
// ✅ FUNCTION: Check Passing
// ===============================

function isPassing(average) {
    return average >= PASSING_GRADE;
}


// ===============================
// 🖨 FUNCTION: Generate Report Card
// ===============================

function generateReportCard(student) {

    console.log("=================================");
    console.log("       📘 Coding Academy");
    console.log("          Report Card");
    console.log("=================================");

    console.log(`Name       : ${student.name}`);
    console.log(`Student ID : ${student.id}`);
    console.log("---------------------------------");

    // Print subject-wise marks
    for (let i = 0; i < subjects.length; i++) {
        console.log(`${subjects[i]} : ${student.grades[i]}`);
    }

    console.log("---------------------------------");

    const average = calculateAverage(student.grades);
    const letterGrade = determineLetterGrade(average);
    const honor = isHonorRoll(average);
    const passing = isPassing(average);

    console.log(`Average Grade : ${average.toFixed(2)}`);
    console.log(`Letter Grade  : ${letterGrade}`);
    console.log(`Honor Roll    : ${honor ? "Yes" : "No"}`);
    console.log(`Result        : ${passing ? "Pass" : "Fail"}`);

    if (honor) {
        console.log("🎉 Congratulations on making the Honor Roll!");
    }

    console.log("=================================\n");

    return {
        average,
        honor,
        passing
    };
}


// ===============================
// 👨‍🎓 STUDENT DATA
// ===============================

const students = [
    {
        name: "Johnson",
        id: "S101",
        grades: [95, 88, 92, 85, 90]
    },
    {
        name: "Alice",
        id: "S102",
        grades: [70, 75, 80, 78, 77]
    },
    {
        name: "Charlie",
        id: "S103",
        grades: [50, 60, 58, 65, 55]
    }
];


// ===============================
// 📊 CLASS STATISTICS
// ===============================

let totalClassAverage = 0;
let honorRollCount = 0;
let passingCount = 0;

for (let i = 0; i < students.length; i++) {

    const result = generateReportCard(students[i]);

    totalClassAverage += result.average;

    if (result.honor) honorRollCount++;
    if (result.passing) passingCount++;
}

const classAverage = totalClassAverage / students.length;


// ===============================
// 📈 FINAL CLASS REPORT
// ===============================

console.log("********* CLASS STATISTICS *********");
console.log(`Class Average        : ${classAverage.toFixed(2)}`);
console.log(`Honor Roll Students  : ${honorRollCount}`);
console.log(`Passing Students     : ${passingCount}`);
console.log(`Total Students       : ${students.length}`);
console.log("************************************");
```

---

# 📈 Final Class Report

Outputs:

* Class Average
* Total Honor Roll Students
* Total Passing Students
* Total Students

---

# 🎯 Key Concepts Covered

| Concept       | Learned                 |
| ------------- | ----------------------- |
| Variables     | let, const, var         |
| Scope         | Block vs Function       |
| Functions     | Reusability             |
| Loops         | for loop                |
| Conditions    | if / else if / else     |
| Arrays        | Storing multiple values |
| Objects       | Storing structured data |
| Return Values | From functions          |

---